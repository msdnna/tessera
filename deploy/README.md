<p align="right"><b>Русский</b> · <a href="README.en.md">English</a></p>

# Tessera — продакшн-развёртывание

Развёртывание в один хост через Docker за Caddy (автоматический HTTPS). Образы
собираются на dev-машине и едут на сервер тарболом — на сервере нет ни исходников,
ни сборочного тулчейна. (Позже: публикация в GHCR и `docker pull` вместо тарбола.)

```
┌────────── VDS (Ubuntu 24.04) ───────────┐
│  Caddy :80/:443  ──TLS──► frontend(nginx)│
│                            │ /api ─► backend (distroless) ─► postgres │
│                    └ /livekit ─► livekit (сигналинг, host-порта нет)  │
│  livekit :7881/tcp + :7882/udp — только медиа звонков                 │
│  наружу открыты 80/443 + медиа-порты; БД и backend — внутренние       │
└──────────────────────────────────────────┘
```

## Файлы

| Файл | Где выполняется | Назначение |
|------|-----------------|------------|
| `build-and-save.sh` | dev-машина | сборка прод-образов → `dist/*.tar.gz` |
| `server-bootstrap.sh` | сервер (однократно) | харденинг ОС + установка Docker |
| `docker-compose.yml` | сервер | прод-стек на готовых образах |
| `Caddyfile` | сервер | TLS-эдж + реверс-прокси |
| `livekit.yaml` | сервер | конфиг SFU видеозвонков (без секретов, они в `.env`) |
| `egress.yaml` | сервер | конфиг записи конференций (нужен только с профилем `recording`) |
| `chrome-sandboxing-seccomp-profile.json` | сервер | seccomp для песочницы Chrome в egress (там же) |
| `.env.example` | сервер | скопировать в `.env`, заполнить секреты |

## Первое развёртывание

**0. DNS** — направьте A-запись (`tessera.example.com`) на IP VDS до шага 4
(Caddy нужно, чтобы имя резолвилось, для выпуска сертификата).

**1. Подготовка сервера** (на VDS, под sudo-пользователем):
```bash
scp deploy/server-bootstrap.sh user@server:/tmp/
ssh user@server 'sudo bash /tmp/server-bootstrap.sh'
# перезайдите в сессию, чтобы применилась группа docker
```

**2. Сборка и доставка образов** (dev-машина):
```bash
bash deploy/build-and-save.sh
scp deploy/dist/tessera-images-*.tar.gz user@server:/opt/tessera/
scp deploy/{docker-compose.yml,Caddyfile,livekit.yaml,.env.example} user@server:/opt/tessera/
```

**3. Настройка** (сервер, `/opt/tessera`):
```bash
cp .env.example .env && chmod 600 .env
# сгенерировать секреты:
#   openssl rand -hex 32      (JWT_SECRET, ENCRYPTION_KEY)
#   openssl rand -base64 24   (POSTGRES_PASSWORD)
nano .env          # задать DOMAIN, ACME_EMAIL, PUBLIC_URL, секреты, теги образов
docker load -i tessera-images-*.tar.gz
```

**4. Запуск**:
```bash
docker compose up -d
docker compose exec backend /migrate      # применить миграции БД
docker compose logs -f                     # смотреть, как Caddy берёт сертификат
```

Откройте `https://tessera.example.com` и **сразу зарегистрируйтесь** — первый
пользователь становится администратором.

## Обновление до новой версии

```bash
# dev-машина
bash deploy/build-and-save.sh
scp deploy/dist/tessera-images-*.tar.gz user@server:/opt/tessera/
# сервер
docker load -i tessera-images-*.tar.gz
# поднять теги BACKEND_IMAGE / FRONTEND_IMAGE в .env до новых версий
docker compose up -d
docker compose exec backend /migrate       # если релиз добавил миграции
```

### Разово: владелец тома с вложениями

Только для развёртываний, созданных **до** версии с исправлением #2820. Бэкенд
работает от `nonroot` (65532), а старым томам `backend_uploads` Docker выставил
владельцем `root` — загрузка картинок и вложений падает с «permission denied».
Новые тома чинятся сами (каталог с нужным владельцем теперь лежит в образе), а
уже существующий надо поправить один раз:

```bash
docker volume ls                                   # найдите <проект>_backend_uploads
docker compose stop backend
docker run --rm -v <проект>_backend_uploads:/d busybox chown -R 65532:65532 /d
docker compose start backend
```

Проверка: в логе бэкенда при старте есть `uploads: каталог доступен на запись`.
Строка `uploads: каталог НЕ доступен на запись` означает, что шаг ещё нужен.

## Видеозвонки (LiveKit)

Звонки и летучки обслуживает **SFU** — контейнер `livekit`: каждый участник шлёт
одну исходящую дорожку на сервер, а не по одной на каждого собеседника. Без него
разговор вчетвером упирается в исходящий канал самого слабого участника.

**Что нужно открыть на фаерволе** (`ufw allow` — `server-bootstrap.sh` этого не
делает, чтобы порты не открывались на хостах без конференций):

| Порт | Зачем | Если закрыть |
|------|-------|--------------|
| `7882/udp` | всё медиа звонков (один порт благодаря UDP-mux) | звонки уходят на TCP-фолбэк: выше задержка, хуже качество |
| `7881/tcp` | ICE поверх TCP — фолбэк там, где UDP режется | участники за строгими корпоративными фаерволами не подключатся вовсе |

```bash
sudo ufw allow 7882/udp && sudo ufw allow 7881/tcp
```

Порт сигналинга **7880 наружу не публикуется** и публиковать его не нужно: он
ходит через Caddy как `https://<DOMAIN>/livekit/*` по уже существующему
сертификату. Это не косметика — на 7880 висит ещё и управляющий HTTP API LiveKit
(создать комнату, выкинуть участника, получить список присутствующих), который
имеет право звать только наш бэкенд.

**Секреты.** `LIVEKIT_API_KEY` / `LIVEKIT_API_SECRET` задаются в `.env` и без них
`docker compose up` намеренно падает с явной ошибкой — дефолтного секрета нет.
Секретом подписываются пропуска участников: кто им владеет, тот войдёт в любую
летучку на этом сервере. Обращайтесь с ним как с `JWT_SECRET`.

**Обновление существующего развёртывания.** Помимо новых образов положите рядом
`livekit.yaml` и добавьте в `.env` четыре `LIVEKIT_*` из `.env.example`, иначе
стек не поднимется.

**Проверка после запуска:** `docker compose logs livekit` — строка `starting
LiveKit server`; `curl -sf https://<DOMAIN>/livekit/` должен ответить (пусть и
ошибкой протокола), а не 502.

## Запись конференций (по желанию, выключена по умолчанию)

Запись делает **сервер**, а не браузер участника: контейнер `egress` заходит в
летучку скрытым участником, рисует сетку в headless Chrome и пишет mp4 прямо в том
`backend_uploads` (`rec/<конференция>/`), откуда его отдаёт обычная ручка скачивания.
Ничего не включив, вы ничего и не платите — оба контейнера сидят за профилем
`recording`, и обычный `docker compose up -d` их не запускает.

**Включение — одна команда, и она меняет две вещи сразу:**

```bash
LIVEKIT_REDIS_HOST=redis:6379 docker compose --profile recording up -d
```

Половинки не работают и ломаются по-разному: профиль без переменной поднимет
egress, до которого SFU не достучится, а переменная без профиля отправит SFU на
несуществующий Redis, и он не стартует вовсе. Выключение — та же команда без обоих.
Держите обе половинки вместе и при каждом обновлении стека: `LIVEKIT_REDIS_HOST` в
`.env` без профиля в команде — это та же ловушка, только отложенная до следующего
`docker compose up -d`.

**Цена, которую надо принять осознанно.** Redis здесь не кэш, а единственный канал
между `livekit-server` и egress: сервер кладёт задание на запись только в
`RedisStore`, для локального хранилища `getEgressStore` отдаёт `nil` — задание
некуда положить, и запись молча не начинается. Обойти это нельзя. А непустой
`REDIS_HOST` переводит SFU в многоузловой режим: состояние комнат уезжает из
процесса в Redis, и его падение роняет **конференции целиком**, не только запись.
Именно поэтому запись — опция развёртывания, а не «всегда включено».

Если запись не включена, кнопка в интерфейсе не исчезает: SFU ответит бэкенду
`egress not connected (redis required)`, и модератор увидит ошибку. Понятное
«запись не настроена» (503) бывает только когда не настроен сам LiveKit.

**Новых портов открывать не надо.** Egress ходит только внутрь compose-сети, а
шаблоны сетки раздаёт сам себе на 7980 внутри контейнера — наружу не уходит ничего,
и третья сторона не узнаёт, когда у вас летучка.

**Место на диске.** Ориентир — около 1 ГБ на час записи комнаты, всё в томе
`backend_uploads` (тот же, что и вложения, — значит и тот же бэкап). Срок хранения
задаётся у каждой конференции при планировании (`recording_ttl_days`, по умолчанию
30 дней; `0` — хранить бессрочно), просроченное удаляет фоновый воркер бэкенда.
Отдельный предохранитель — `session_limits.file_output_max_duration: 2h` в
`egress.yaml`: запись, у которой забыли нажать «Стоп», иначе шла бы всю ночь.

**Проверка после запуска:** `docker compose --profile recording ps` — `tessera-egress`
в состоянии `Up`, а `tessera-egress-init` `Exited (0)` (он один раз готовит каталог
`rec/` под двух писателей — бэкенд и egress — и больше не нужен). Настоящая проверка
— живая: включите запись в идущей летучке, красная точка должна появиться **у всех**
участников, а после «Стоп» запись появляется в списке под комнатой за несколько
секунд. Если она появляется со статусом «не удалась», смотрите `docker compose logs
egress` — по умолчанию он молчит на уровне `warn`, ошибки видны.

## Бэкапы (делайте — без них конфиденциальность неполна)

```bash
# дамп БД (cron, например ночью)
docker compose exec -T postgres pg_dump -U tessera tessera | gzip > backup-$(date +%F).sql.gz
# вложения лежат в томе backend_uploads — его тоже в бэкап.
```
Шифруйте дампы (`gpg`) и храните их **вне хоста** (объектное хранилище / другой
сервер). Периодически проверяйте восстановление. Перед каждым обновлением снимайте
снапшот диска VDS.

## За корпоративным прокси

Если Tessera стоит за прокси/балансировщиком, которым вы не управляете (корпоративный
эдж, API-gateway), именно он — а не встроенные Caddy/nginx — определяет задержки и
работу WebSocket. Чтобы живая доска и быстрые ответы работали и там, нужно:

- **WebSocket-upgrade** на `/api/ws`: пробрасывать заголовки `Upgrade` / `Connection`
  и ходить к апстриму по HTTP/1.1. Без этого realtime-сокет не подключится, а клиенты
  скатываются в тихое устаревание данных + постоянные переподключения.
- **Idle-timeout ≥ 60с** на этом маршруте (лучше минуты). Бэкенд пингует каждые 25с,
  чтобы держать сокет открытым; прокси, обрывающий простаивающие соединения быстрее,
  будет раз за разом ронять живые обновления доски.
- **Сжатие ответов** (`gzip`/`br`) для `application/json`, ЛИБО пропускать собственный
  gzip бэкенда нетронутым (не срезать `Accept-Encoding` на входе и `Content-Encoding`
  на выходе). Tessera сама gzip-ует свой JSON; пейлоады доски / журнала синка
  сжимаются ~в 10 раз — это разница между суб-секундной и многосекундной загрузкой на
  узком канале.
- **Keep-alive до апстрима**: переиспользовать соединения к контейнеру фронтенда, а не
  открывать новый TCP + TLS на каждый запрос — открытие доски выстреливает ~10 вызовов
  разом, и посоединенный setup иначе доминирует.

Встроенный `frontend/nginx.conf` уже делает всё перечисленное для встроенного пути —
повторите эти настройки на внешнем прокси.

## Тюнинг Postgres под хост

`docker-compose.yml` едет с консервативными настройками Postgres под ~2ГБ-хост.
На более крупном VDS поднимите их в `.env` (затем `docker compose up -d postgres`):

```
PG_SHARED_BUFFERS=1GB           # ~25% RAM
PG_EFFECTIVE_CACHE_SIZE=3GB     # ~50-75% RAM
PG_WORK_MEM=64MB
PG_MAINTENANCE_WORK_MEM=512MB
PG_RANDOM_PAGE_COST=1.1         # SSD; оставляйте 4 только для «блинов»
```

## Модель безопасности (встроено)

- У Postgres, backend и сигналинга LiveKit (7880, вместе с его управляющим API)
  **нет host-портов** — недостижимы из интернета by design. Наружу у LiveKit
  торчат только медиа-порты 7881/7882, по которым не отдаётся ничего, кроме
  зашифрованных дорожек уже авторизованного участника.
- Образ backend — **distroless, non-root**, статический бинарь.
- `APP_ENV=production` **fail-closed** без `JWT_SECRET` / `ENCRYPTION_KEY` /
  `DATABASE_URL` / `PUBLIC_URL`.
- TLS повсюду через Caddy (авто-обновление Let's Encrypt).
- SSH: только по ключу, root-логин выключен, fail2ban; ufw открывает лишь 22/80/443
  (плюс 7881/tcp и 7882/udp, если включаете звонки — см. раздел про LiveKit).
