version: "2"

linters:
  default: standard
  enable:
    - gocritic
    - revive
    - misspell

  exclusions:
    rules:
      # Test files: relax error-check and style noise.
      - path: _test\.go
        linters:
          - errcheck
          - gocritic
