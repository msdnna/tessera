---
bump: patch
---

- **fix(web): выделение ячеек таблицы больше не закрашивает всю рабочую область документа
  (#2739).** Подсветка выделенных ячеек рисуется абсолютным `::after` с `inset: 0`, но у
  `td`/`th` не было `position: relative` — ближайшим позиционированным предком оказывалась
  рабочая область документа, и подсветка растягивалась на неё целиком вместо выбранных
  ячеек. Ячейка теперь сама задаёт содержащий блок.
