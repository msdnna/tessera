---
bump: patch
---
- **fix(api): в объекте пользователя отдаётся `created_at` (#2749).**
  Веб-клиент по нему решает, показывать ли «Что нового»: только аккаунтам,
  которые существовали до текущей сборки (реально обновились), а не свежим
  регистрациям.
